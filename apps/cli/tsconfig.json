{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // React types need the DOM lib; OpenTUI's JSX types extend React's.
    "lib": ["ESNext", "DOM"],
    "jsx": "react-jsx",
    "jsxImportSource": "@opentui/react"
  },
  "include": ["src"]
}
